# Intermediate Data

This folder contains datasets transformed from the original World Bank files into long format.

Each dataset was reshaped so that every row represents one country-year observation. These intermediate tables were then used in SQL to create the final merged dataset for analysis.
